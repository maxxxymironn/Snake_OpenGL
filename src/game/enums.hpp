#pragma once

enum class GameStatus {
    MENU,
    GAME_START,
    GAME,
    WIN,
    LOOSE,
};

enum class GameMode {
    DEFAULT,
    THROUGH_WALLS,
    BOUNDLESS
};

enum class Color {
    WHITE,
    BLACK,
    GRAY,
    RED,
    GREEN,
    BLUE,
    AQUA,
    PURPLE,
    PINK,
    ORANGE
};